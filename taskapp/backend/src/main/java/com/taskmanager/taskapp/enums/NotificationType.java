package com.taskmanager.taskapp.enums;

public enum NotificationType {
    SYSTEM_TEXT,     // Simple text notifications
    DAILY_BRIEFING,  // Complex JSON data for the dashboard report
    TASK_REMINDER    // Reminders for upcoming tasks
}